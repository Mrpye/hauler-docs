## hauler package add action parse_token

This command adds action parse_token to the package manifest

### Synopsis


Description:
This command adds action parse_token to the package manifest

The copy action allows you to parse golang tokens in a file

Example Command:
```
hauler package add action parse_token parse_token
```
Result Example:
```
actions:
	- key: parse_token
	description: Add a description here
	action: action_parse_token
	config:
		dest_file: ""
		source_file: ""
```
Parameters:
- source_file: source file to parse
- dest_file: destination where to save the results

Other Parameters
```
# Run the below command to see help regarding result_action, result_format, target_action
hauler package add action -h
```



```
hauler package add action parse_token [name] [flags]
```

### Options

```
  -d, --desc string      Description
  -h, --help             help for parse_token
  -o, --override         Target Override
  -p, --profile string   App Profile
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.hauler/config.json)
```

### SEE ALSO

* [hauler package add action](hauler_package_add_action.md)	 - Adds an action to the package manifest

###### Auto generated by spf13/cobra on 17-Dec-2022
